# Core requirements (unpinned list for convenience)
numpy
pandas
matplotlib
seaborn
scikit-learn

# Optional / helpful extras
# Optional for interactive plots
plotly
# Optional for association rule mining
mlxtend

# Pinned core versions (generated from development environment)
# These are the exact versions used when the notebook was last executed successfully.
# If you need strict reproducibility, install from these pinned versions.
numpy==1.26.4
pandas==2.2.2
matplotlib==3.9.2
scikit-learn==1.5.1
mlxtend==0.23.4